{
  "name": "@fuseos/proto",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "proto:gen": "echo \"@fuseos/proto: no TS bindings — Android and macOS generate their own from proto/\" && exit 0"
  }
}
